Task 1: Successfully executed via pickle.ipynb. The valid account and transaction pickles, which are also recognized on Github, were generated and utilized throughout the entire task.

Tasks 2 & 3: Both tasks are covered at the start of the four .ipynb files mentioned below.

Task 4: This was tackled using various methods and models, culminating in four distinct .ipynb files. Their specifics are as follows:

-SARIMA balance: Using the SARIMA model, we trained on the provided balance graphs and performed forecasts for intervals like 30 and 90 days.
-SARIMA inflow outflow: This SARIMA model was trained using inflow-outflow values. Following this, we generated balance graphs for accounts based on the forecasted inflow-outflow data.
-MLP balance: The MLP model was trained using balance graphs, forecasting for periods such as 30 and 90 days.
-MLP inflow outflow: Here, the MLP model was trained on inflow-outflow values. We then constructed balance graphs for accounts using the forecasted data.

Execution Instructions:
To execute the codes, input the desired account ID in the designated sections. Ensure to run each cell sequentially to visualize graphs and results.

Additional Information:
A comprehensive report detailing the evaluation metrics, chosen features, trade-offs, and other relevant aspects has been uploaded separately.


